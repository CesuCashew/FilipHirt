{
  "name": "filip-hirt-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website with contact form integration",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build required'",
    "setup-db": "node netlify/functions/setup-database.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "dotenv": "^16.6.1",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
